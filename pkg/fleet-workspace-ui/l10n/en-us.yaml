fleet-workspace-ui:
  manageUsers: Manage Users
  remove: Remove
  add: Add
  addFleetMemberDialog: Add Fleet Member
  members:
    addMember: Add Fleet Member
    fleetPermissions:
      label: Fleet Permissions
      description: Controls what access users have to the Fleet workspace
  roles:
    admin:
      label: Admin
      description: Admin have full control over the Fleet workspace and all resources inside it.
    editor:
      label: Editor
      description: Editor can manage the resources inside the Fleet workspace.
    view:
      label: View
      description: View is read only for Fleet workspace.